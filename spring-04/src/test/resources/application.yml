
spring:
  messages:
    basename: i18n.messages
    fallback-to-system-locale: false
  shell:
    interactive:
      enabled: false

test:
  locale-name: ru-RU
  csv-path: questions_ru_RU.csv
  limit: 3